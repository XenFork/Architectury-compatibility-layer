pluginManagement {
    repositories {
        gradlePluginPortal()
        mavenCentral()
        mavenLocal()
    }
}

plugins {
    id "acl" version "$acl_test_version"
}

rootProject.name = 'acl'

include("core")
project(":core").name="acl-core"

